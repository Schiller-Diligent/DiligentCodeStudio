# Final Patch: Remove Remaining Guide Me Text (v0.7.0-dev)

This package removes the remaining hidden `Guide me` prompt text from `src/App.tsx` so the GitHub update safety check passes.

Visible UI changes remain the same:

- The top toolbar `Guide Me` button is removed.
- The duplicate on-page `What this page does` / `Guide Me` guide area is removed.
- The top toolbar keeps `Manual`, `Setup`, and `AI Help`.
- The bundled Operator/User Manual Revision 1.1 remains included.
- The CI npm registry/retry fix remains included.
