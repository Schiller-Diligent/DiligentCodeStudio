name: Build Diligent Code Studio Cross-Platform

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows
            os: windows-2022
          - name: Linux
            os: ubuntu-22.04
          - name: macOS
            os: macos-14

    env:
      CI: true
      CARGO_INCREMENTAL: 0
      CARGO_TERM_COLOR: always
      NPM_CONFIG_AUDIT: false
      NPM_CONFIG_FUND: false
      NPM_CONFIG_UPDATE_NOTIFIER: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20.12.2

      - name: Show Node and npm versions
        shell: bash
        run: |
          echo "Node version:"
          node --version
          echo "npm version:"
          npm --version
          echo "Runner OS:"
          echo "${{ runner.os }}"
          echo "Runner temp:"
          echo "$RUNNER_TEMP"

      - name: Configure npm for CI
        shell: bash
        run: |
          npm config set cache "$RUNNER_TEMP/npm-cache" --global
          npm config set fetch-timeout 300000 --global
          npm config set fetch-retry-maxtimeout 300000 --global
          npm config set fetch-retry-mintimeout 30000 --global
          npm config set fetch-retry-factor 10 --global
          npm config set fetch-retries 5 --global
          npm config set audit false --global
          npm config set fund false --global
          npm config get cache

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Show Rust versions
        shell: bash
        run: |
          rustc --version
          cargo --version

      - name: Install Linux Tauri dependencies
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Clean dependency and build folders
        shell: bash
        run: |
          echo "Cleaning old dependency/build folders..."
          rm -rf node_modules
          rm -rf dist
          rm -rf src-tauri/target/debug
          rm -rf src-tauri/target/release/bundle
          npm cache clean --force || true

      - name: Install frontend dependencies with retry
        shell: bash
        timeout-minutes: 12
        run: |
          set +e

          echo "Clearing npm cache before install..."
          npm cache clean --force || true

          echo "First attempt: npm install"
          npm install --no-audit --no-fund --prefer-online --timeout=300000 --fetch-timeout=300000
          FIRST_EXIT=$?

          if [ $FIRST_EXIT -ne 0 ]; then
            echo "npm install failed with exit code $FIRST_EXIT"
            echo "Cleaning node_modules and npm cache..."
            rm -rf node_modules
            npm cache clean --force || true

            echo "Second attempt: npm install with --legacy-peer-deps"
            npm install --no-audit --no-fund --prefer-online --timeout=300000 --fetch-timeout=300000 --legacy-peer-deps
            SECOND_EXIT=$?
          else
            SECOND_EXIT=0
          fi

          if [ $SECOND_EXIT -ne 0 ]; then
            echo "Dependency installation failed after all attempts."
            exit 1
          fi

          echo "Dependency installation step completed."

      - name: Verify frontend dependencies
        shell: bash
        run: |
          echo "Verifying node_modules..."
          if [ ! -d "node_modules" ]; then
            echo "ERROR: node_modules folder is missing."
            exit 1
          fi

          echo "Checking for TypeScript compiler..."
          if [ ! -f "node_modules/.bin/tsc" ] && [ ! -f "node_modules/.bin/tsc.cmd" ]; then
            echo "ERROR: TypeScript compiler was not found."
            echo "Listing node_modules/.bin for diagnostics:"
            ls -la node_modules/.bin || true
            exit 1
          fi

          echo "Checking for Vite..."
          if [ ! -f "node_modules/.bin/vite" ] && [ ! -f "node_modules/.bin/vite.cmd" ]; then
            echo "ERROR: Vite was not found."
            echo "Listing node_modules/.bin for diagnostics:"
            ls -la node_modules/.bin || true
            exit 1
          fi

          echo "Frontend dependencies verified."

      - name: Build frontend
        shell: bash
        run: |
          echo "Building frontend..."
          npm run build

      - name: Build Tauri app
        shell: bash
        run: |
          echo "Building Tauri app for ${{ matrix.name }}..."
          npm run tauri:build

      - name: List bundle output
        if: always()
        shell: bash
        run: |
          echo "Bundle output:"
          find src-tauri/target/release/bundle -maxdepth 5 -type f -print || true

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: diligent-code-studio-${{ matrix.name }}
          path: |
            src-tauri/target/release/bundle/**
          if-no-files-found: error